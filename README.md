Payroll-management is a python application ,which uses flask library to connect with the database .I am rendering a HTML template which will get the input from the user and feed it to MySQL database.
